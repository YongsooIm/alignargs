class Config {
    constructor(rightAlignDecimal, replace, trimTrail, formatHex, padType) {
        this.rightAlignDecimal = rightAlignDecimal;
        this.replace = replace;
        this.trimTrail = trimTrail;
        this.formatHex = formatHex;
        this.padType = padType;
    }
}
